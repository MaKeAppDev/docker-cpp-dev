sudo: required

services:
  - docker

install:
  - docker build -t makeappdev .
  - docker run -d makeappdev

script:
  - docker ps -a
  - docker run makeappdev /bin/sh -c "cmake --version; g++ --version; doxygen --version; lcov --version; valgrind --version"

notifications:
  email: false
